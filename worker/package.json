{
  "name": "mibox-houston-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 execute mibox-houston --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute mibox-houston --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.24.0"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "otpauth": "^9.2.0"
  }
}
